covariate-shift-detector:
  description: A MMDrift detector from Alibi-detect library that detects if there
    is a covariate shift between the training and test dataset for ML model.
  labels:
  - covariate-shift-detector
  path: models/covariate_shift_detector.joblib/
  type: model
ml-bike-sharing:
  description: Random Forrest Classifier for bike sharing dataset
  labels:
  - bike-sharing-model
  path: .mlem/model/ML_bike_sharing
  type: model
